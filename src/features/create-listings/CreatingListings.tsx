import ListingForm from "../../components/ListingForm";

const CreatingListings = () => {
    return (
        <section>
            <div className="bg-white w-[30rem]">
                <ListingForm>
                    <h2 className="text-2xl my-4 font-bold text-center">
                        Create Listing
                    </h2>
                </ListingForm>
            </div>
        </section>
    );
};

export default CreatingListings;
